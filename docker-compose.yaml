version: "3"
services:

  app:
    build:
      context: .
      dockerfile: Dockerfile
    # env_file:
    #   - ./.env
    working_dir: /usr/src/app
    ports:
      - 3000:3000
    volumes:
      - ./:/usr/src/app
    # Overrides default command so things don't shut down after the process ends.
    command: /bin/sh -c "grunt && WS_SECRET="4Qfj80ZXtfAhHjyEqN1Aoq7MvMZnSnwlr0SCScp4zqXvqynrmXoN7WeQyyYXgY" npm start"
